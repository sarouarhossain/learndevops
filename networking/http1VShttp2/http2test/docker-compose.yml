version: "2"

services:
  nginx:
    image: nginx
    restart: always
    ports:
    - "80:80"       
    - "443:443"
    #- "8888:8888"
    volumes:
      - "/etc/letsencrypt:/etc/letsencrypt/"      
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
      - "./html:/usr/abc/html:ro"
