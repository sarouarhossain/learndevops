### check ninx version
`nginx -v`  

### install nginx
`sudo apt update`  
`sudo apt install nginx`

### start nginx
`sudo nginx`

### stop nginx
`sudo nginx -s stop`

### reload
`nginx reload`

`sudo lsof -i TCP:80`



